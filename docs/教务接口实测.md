# 教务系统接口实测记录（M1 · 全部打通 ✅）

> 实测日期：2026-09-02　|　实测人：时栖 + WorkBuddy　|　样本已存 docs/probe/

## 0. 完整链路总览（App 端照此实现）

```
① POST https://ehallmobile.xyc.edu.cn/api/v4/api/login     （3DES 加密，见登录协议分析.md）
   → { code:"200", token, data:{ CASTGC, JSESSIONID, casUserInfo } }
   → 后续门户 API 带 Authorization: <token> 头

② GET https://zfjwxt.xyc.edu.cn/sso/xyoauthlogin?ticket=<token>
   → 302 /jwglxt/ticketlogin?uid=<学号>&timestamp=...&verify=<服务端签名>&ydType=1
   → 302 /jwglxt/xtgl/login_slogin.html（中转）
   → 302 /jwglxt/xtgl/index_initMenu.html?jsdm=xs → 200 教务首页 ✅
   → 教务域 JSESSIONID 建立（OkHttp CookieJar 自动持有）
   ★ index_initMenu 响应 HTML 内联完整菜单（clickMenu 调用，含 wapLogin 签名 key/time）

③ 业务接口：两条路径
   A) 直连：POST /jwglxt/kbcx/xskbcx_cxXsKb.html?gnmkdm=N2151  {xnm, xqm}   ← 课表（最简单）
   B) WAP 签名链：GET /jwglxt/xtgl/login_wapLogin.html?procode=008&type=1
        &choice=<Y功能码>&uid=<学号>&role=xs&key=<内联签名>&time=<内联时间戳>
      → 302 对应业务移动端页面（页面内含数据接口与 pkey）
```

## 1. 课表（已验证 ✅）

```
POST https://zfjwxt.xyc.edu.cn/jwglxt/kbcx/xskbcx_cxXsKb.html?gnmkdm=N2151
Headers: X-Requested-With: XMLHttpRequest, Referer: .../xtgl/index_initMenu.html
Form:    xnm=<学年起始年,如2025>  xqm=<3|12|16 对应第1/2/3学期>
返回 JSON: {
  kbList: [ { day:"2"(周一=1), jc:"1-2节", jcor:"1-2", cdmc:"主B506"教室,
              jxbmc:"工程力学B-0001"教学班, kch_id, kcmc(在后续字段), jsxm教师,
              zc 周次串(1-16周), xqj, ... } × 20~30 条 ],
  xsxx: { XM姓名, XH学号, BJMC班级, ZYMC专业, NJDM_ID年级, XNMC学年, KCMS... }
}
```
- 样本：`probe/kb_2025_3.json`（20条）、`probe/kb_2025_12.json`（30条）
- `xsxx` 顺带解决个人信息基础字段
- 当前学期(2026-3) kbList 为空 = 尚未排课，正常

## 2. 成绩（已验证 ✅）

```
②' GET /jwglxt/xtgl/login_wapLogin.html?procode=008&type=1&choice=Y305005
     &uid=<学号>&role=xs&key=<签名>&time=<时间戳>     ← 签名从 index_initMenu 内联菜单取
   → 302 /jwglxt/cjcx/cjcxMobile_cxDgXscj.html       （成绩移动端页面）

POST /jwglxt/cjcx/cjcxMobile_cxXsgrcj.html?doType=app
Form: xnm=<学年>  xqm=<3|12|16>  pkey=（页面 hidden 初始为空，空值直接发）
返回: [ { cj:"85"成绩, jd:"3.50"绩点, kcmc, kch, kcxzmc"公共必修", kclbmc,
          xf 学分, jsxm 教师, kkbmmc 开课学院, bj 班级, ksxz, cjsfzf ... } × N ]
```
- 样本：`probe/cj_mobile.json`（2025-3: 10 条 / 2025-12: 13 条）
- 关键坑：功能码是 **Y305005**（学校自定义 Y 前缀），默认 N305005 返回"没有访问权限!"

## 3. 菜单全表（index_initMenu 内联 clickMenu，App 功能地图）

| choice 功能码 | 功能 | 备注 |
|---|---|---|
| Y011010 | 学生通知公告查询 | |
| **Y100805** | **个人信息查询** | 待实测，流程同成绩 |
| Y102015 | 学籍异动审批状态 | |
| **Y210501** | **校历查询** | 课表周次计算依据，待实测 |
| Y219905 | 空教室查询 | 扩展功能 |
| Y2510 | 等级考试报名 | |
| Y253510 | 学生课表查询(旧) | |
| Y253511 | 学生课表查询(新) | 已用直连端点替代 |
| **Y305005** | **学生成绩查询** | 已验证 |
| **Y357005** | **考试查询** | 待实测，流程同成绩 |
| Y401644 | 学生评价 | |

## 4. 第三方应用列表（ehall 聚合入口数据源，已验证 ✅）

```
GET https://ehallmobile.xyc.edu.cn/api/v4/api/app/getApplication   （Authorization 头）
→ 26 个应用 { name, href, hrefType }
打开方式: href + "?ticket=<门户token>"（hrefType!=3/4/5）
```
含教务/学工(ssxt)/离校(lxxt)/缴费(wszf)/图书馆(超星)/网络教学(超台)/邮箱等。

## 5. 已知限制与注意事项

- wapLogin 的 key/time 签名由 index_initMenu SSR 生成，**每次 SSO 后重新提取**，不可复用
- 成绩/个人信息/考试等 WAP 链接口需先 GET 对应移动端页面（Referer 校验）
- 请求频率保持克制（每次 App 启动登录一次 + 会话失效重登；数据本地缓存优先）
- 教务会话过期表现：302 → login_slogin.html；门户 token 过期：API code=401
